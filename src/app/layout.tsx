// RETRO Qatar — Root Layout
// Minimal root layout that delegates to [locale]/layout.tsx for locale-specific rendering

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
